#include "main.h"
/**
 * mul - Checks for lowercase charcters
 * @a: The characters to be checked
 * @b: 1 for lowercase characters or 0 for anything else
 * Return: 1 for lowercase characters or 0 for anything else
 */
int mul(int a, int b)
{
int c = a * b;
return (c);
}
