#include "main.h"
/**
 * add - Checks for lowercase charcters
 * @a: The characters to be checked
 * @b: The characters to be checked
 * Return: 1 for lowercase characters or 0 for anything else
 */

int add(int a, int b)
{
int r = a + b;
return (r);
}
