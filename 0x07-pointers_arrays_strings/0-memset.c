#include "main.h"
/**
 * _memset - hghkh
 * @s: kdfk
 * @b: egdfg
 * @n: fds
 * Return: fsfd
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int l = 0;
while (l < n)
{
s[l] = b;
l++;
}
return (s);
}
